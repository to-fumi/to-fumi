name: Update badges
on:
  schedule:
    # 毎日午前2時（UTC）に実行
    - cron: "0 2 * * *"

jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
    - name: Badges - Readme
      uses: pemtajo/badge-readme@main
      with:
        CREDLY_USER: fumiya-toyama